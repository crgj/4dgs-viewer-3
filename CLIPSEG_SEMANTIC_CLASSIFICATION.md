# CLIPSeg 多视角 Gaussian 语义分类插件

<!-- #WDD-gpt 2026-08-26 - 记录纯前端模型依赖、缓存和分类边界，避免把实验插件误解为服务端语义重建。 -->

## 运行边界

- 核心推理、随机相机规划、概率回投和 Tag 写入全部在浏览器前端完成，不依赖 Node.js、Python、CUDA、localhost 服务或云端推理 API。
- 推理运行在独立 Web Worker 中；渲染视口负责抓取均匀分布的时空视角，并按每个视角对应帧的位置与透明度把分割概率投影回稳定 Gaussian ID。
- 插件会在下载和建图前调用 `requestAdapter()` 并检查 `shader-f16`：能力完整时使用 q4f16/WebGPU；没有 Adapter 或缺少 f16 能力时自动使用 q8/WASM。场景原有 WebGPU/WebGL2 渲染后端不需要改变。
- 实测 q4f16 图直接在 WASM 运行会在 LayerNorm 融合阶段创建 Session 失败，因此兼容路径使用同一模型 revision 的官方 q8 图，不把 q4f16 伪装成 CPU 可执行模型。
- 当前版本分类活动驻留片段。静态场景只改变空间视角；动态场景自动把采样分配到多个时间帧。分类结果写入该片段的 CPU-only `semantic.class_id` 与 `semantic.confidence` 属性，不修改位置、旋转、尺度、透明度或 SH。
- 语义属性当前属于编辑会话数据；现有 4CGS 编码器尚未定义通用自定义属性块，因此本插件不暗中改写 4CGS 格式。

## 模型与网络依赖

- 模型：`Xenova/clipseg-rd64-refined`
- 固定 revision：`924dc94f85f58739f353f94258b33bc47eae4862`
- WebGPU 主路径：`onnx/model_q4f16.onnx`，115,520,184 bytes（十进制 115.520184M），数据类型 `q4f16`。
- CPU 兼容路径：`onnx/model_quantized.onnx`，139,115,836 bytes（十进制 139.115836M），数据类型 `q8`。
- 运行时：`@huggingface/transformers@4.2.0`；主路径通过 ONNX Runtime WebGPU，兼容路径在独立 Worker 内通过 ONNX Runtime WASM 执行。
- 本地构建资源：ONNX Runtime 的 `ort-wasm-simd-threaded.asyncify-*.wasm` 为 23,567,045 bytes（十进制 23.567045M），随静态网页部署、由内容哈希长期缓存，不从模型站点重复下载。
- 首次打开插件会从 Hugging Face 下载固定 revision 的配置、Tokenizer、Processor，以及当前浏览器能力对应的一份 ONNX 权重；网络失败会在插件状态区显示具体原因。

## 缓存策略

- `env.useBrowserCache = true`，Transformers.js 使用浏览器站点 Cache Storage 持久缓存模型文件。
- 缓存键包含模型 ID、固定 revision 和文件路径；相同版本刷新页面或重新打开插件不会重复下载权重。
- 升级模型时必须先修改固定 revision；新 revision 使用新缓存键，只有新模型完整加载成功后才成为运行时实例，不会删除旧缓存。
- 离线复用：至少成功在线加载一次后，相同站点来源、相同 revision 可从浏览器缓存复用。浏览器隐私模式、站点数据自动清理或存储配额回收会使缓存失效。
- 用户清理方法：浏览器“站点设置 → 存储空间 → 清除本站数据”。这会同时清除该站点的其他 Cache Storage/IndexedDB/OPFS 数据，界面已明确提示这一影响范围。
- 同一站点如果先后在 WebGPU 和 WebGL2/CPU 模式运行，两份权重会使用不同文件路径分别缓存；插件不会在切换后删除另一份仍可用缓存。
- Cache Storage 按完整 Origin 隔离，`localhost:5173` 与 `localhost:5174` 是两个缓存空间。开发命令已固定 `5173 --strictPort`，端口被占用时必须关闭旧服务，不会再自动换端口触发重复下载。
- 初始化不使用固定总下载时限：只要持续收到字节进度就会续期，仅连续 5 分钟无下载/建图进度才报错。单视角推理使用独立的连续 3 分钟无响应保护。

## 分类算法

1. 以用户当前 Orbit target、构图跨度和观察方向为起点。
2. 用固定 Fibonacci 候选球面与最远点采样生成 2～24 个均匀空间视角；动态场景再以约 `sqrt(采样数)` 个时间点做一维最远点采样，第一时间点固定为用户当前帧，其余覆盖时间轴两端与最大空档。每个时间点保留多个空间角度，避免用单侧图像判断遮挡表面。
3. 每个时空相机位置先切换到对应帧，再等待两个完整渲染帧，使 Gaussian 深度排序稳定后抓取图像；全部视角冻结完成后恢复用户原帧并装载 CLIPSeg，减少模型与 WebGL 同时争用内存造成的上下文丢失。
4. CLIPSeg 对所有 Prompt 一次推理，输出每个 Tag 的 352×352 sigmoid 概率图；由于这些 Prompt 是相互独立的二分类响应，先按每张图、每个 Prompt 的中位数和 98% 分位数做稳健校准，平坦响应不产生伪高分，再进行类别间比较。任务结束后销毁 ONNX Session，权重文件仍留在 Cache Storage 供下次快速建图。
5. 每张概率图只使用该图对应时间帧的 Gaussian 位置和烘焙透明度；4GS 的两个位置 bank 按每个点自己的 `lifetimeMu±lifetimeW` 解释，禁止误当成全局首尾帧插值。先建立点中心深度层，再保留接近前表面的点。
6. 每个可见点按平方根 alpha 加权累积多视角概率；至少两个视角支持、最高平均概率达到阈值且类别间距足够时，成为高可信三维传播种子。阈值同时决定直接分类和是否允许建立传播场。
7. 只有高可信种子才能写入 64³ 局部语义体素场；在至少一个采样帧达到 Alpha 0.01、落入用户构图但被前表面遮挡的点，仅可使用两层以内邻近可靠种子的局部三维补全，并要求局部类别胜出间隔达到 0.05。没有邻近种子、所有采样帧都未出现、完全在构图外或非有限的点保持未识别，禁止再由低置信点或全局多数类别强行填充。
8. `semantic.class_id` 使用 `u16`；安全分类点写入非零类别，已软删除点和无时空证据点保持 0。`semantic.confidence` 使用 `u8`，1～255 表示最终证据强度；UI 显示安全覆盖、未识别、高可信直接证据、三维补全、空间视角和时间帧数量。

## 已知限制

- 这是由渲染图像回投与局部三维传播得到的概率语义，不是直接训练三维网络。薄片、强透明、高反射或严重遮挡点可能保持未识别或获得低置信邻域类别，用户仍应通过类别选择和聚焦检查结果。
- 如果结果显示“直接证据 0”，分类器会返回 0 个分类点而不是制造整模型类别；应先把主体放大并放在当前视口中央，或降低可信种子概率后重新分析。
- 深度可见性目前使用 Gaussian 中心与场景尺度容差近似，不等价于逐像素完整 alpha contribution buffer。
- CLIPSeg 对简洁英文 Prompt 通常比任意中文 Prompt 稳定；界面将显示名称和模型 Prompt 分开保存。
- 多个 Prompt 的 sigmoid 概率并非互斥；本版已校准不同 Prompt 的整体概率偏置，但仍会在用户提供的 Tag 中选择最高综合证据类别。若 Tag 没有覆盖场景语义（例如只有“人物/地面/背景”，但场景包含乐器），未被拒绝的点仍只能归入最接近的已有类别，因此应使用互斥且覆盖目标对象的 Prompt。
- q8/WASM 兼容模式优先保证可运行，速度会明显慢于 q4f16/WebGPU；模型推理在 Worker 内执行，不阻塞主线程视口交互。

## 真实浏览器验收（v3.0.97）

- 场景：`public/4cgs/music.4cgs`，219,489 个活跃 Gaussian，当前帧 0，CPU/WASM q8，16 个均匀视角。
- Prompt：`person`、`handpan musical instrument`、`background`；可信种子概率 0.55。
- 两个复现种子 `20260827` 与 `20260828` 使用相同物理视角集合，结果完全一致：覆盖 219,489/219,489（100.0%），人物 212,919，手碟乐器 6,570，背景 0，平均置信度 64%，高可信直接证据 19,623，三维补全 199,866。
- 点击“手碟乐器”得到 6,570 个稳定 ID，执行 `F` 聚焦后相机准确构图到手碟主体区域，证明类别选择并非只完成计数写入。

## 真实浏览器验收（v3.0.100）

- 场景：`/home/crgj/wdd/data/Row4D/4gs/scene.4gs`，908,268 个 Gaussian、30 帧，CPU/WASM q8，8 次时空采样覆盖 3 个时间帧，默认可信种子概率 0.55。
- 修复前在直接证据为 0 时仍把 739,183 点全部三维补全，且全部落入背景类；安全门修复后同一无种子条件得到分类 0、补全 0、未识别 908,268，证明不会再制造整模型错误类别。
- 加入 Prompt 分位数校准和 64³/两层局部传播后，真实运行得到安全分类 53,856（5.9%）、未识别 854,412、直接证据 17,453、三维补全 36,403；人物 19,526、地面 2,693、背景 31,637。
- 点击人物类别选择 19,526 个稳定 ID，再执行 `F` 聚焦，视口落到画面中央的连续人物轮廓；选择规模相较未校准/宽传播结果的 27,900 点收缩约 30.0%，大面积邻域扩散得到抑制。
