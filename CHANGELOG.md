# 更新日志

本文档按版本倒序记录 Dong Editor 3 的功能、交互与稳定性修改。根目录 `VERSION` 是页面显示的唯一版本来源。

## 3.0.71 - 2026-08-20

- 新增 EasyTimeGS++ 一体 `.4gs` v2/v3 导入：逐块校验 binary PLY，v3 同时校验 `ET4GSM03` footer、JSON metadata 与 CRC32；每个文件独立采用自身帧数和 30 FPS 元数据，真实 `scene.4gs` 的 908,268 点、30 帧和 30 组逐帧活跃数量均与内嵌诊断完全一致。
- `.4gs` 在 Loader Worker 中直接进入现有 Float32 Canonical RAM 和统一渲染路径；位置只保存每个高斯自己生命周期起止时刻的两个端点，运行时按逐点时间插值，避免把短生命周期速度外推到全局首尾帧造成巨大坐标和量化溢出；非线性生命周期透明度按真实 30 帧烘焙，静态旋转、缩放、DC 与 SH 保持单银行。
- 4CGS V2.6 导出保存 `frameRate`、`positionTiming` 和 `opacityTiming` 并在解码后恢复；SH0/SH1/SH2 只在编码工作副本补零到 SH3，不扩大或改写源场景内存。真实 192.574M `.4gs` 使用 261.581M Canonical RAM，54.13 秒输出 38.469M 4CGS，容器清单回读与源数组不变检查通过。
- 欢迎页随机粒子速度上限抽为独立可测试逻辑：随机态在 60 FPS 下约为 6.35–16.20px/s，鼠标排斥和随机目标吸附也不能突破上限；巨大文字仍会先完整解体、全屏散开，保持随机态 10 秒后再聚合。

## 3.0.70 - 2026-08-20

- 修正文字解体末段过早触发慢速上限、同时仍被字形吸附拉回的问题：2.4 秒旋转解体期间完全退出文字吸附且不限制速度，改由最高 0.009 的独立全屏目标吸附配合最高 0.09 的径向爆发，使全部粒子先彻底脱离字形、覆盖画布后才进入慢速随机态。
- 约 6–17px/s 的速度限制现在只在解体值归零后生效；鼠标靠近产生的粒子连线、脉冲光环与排斥力完整保留，随机态中的交互速度仍受安全上限约束。

## 3.0.69 - 2026-08-20

- 文字解体进入随机态后，为每颗粒子施加基于深度与尺寸的速度硬上限，60 FPS 下约为 6–17px/s；解体残余速度、随机目标吸附和鼠标排斥都经过同一限制，不再出现少量粒子快速飞射。
- 粒子尺寸改为三级随机分布：70% 为 0.34–1.12px 星尘、23% 为 1.15–2.20px 中型亮点、7% 为 2.25–3.75px 柔光大粒子；大粒子自动获得外层微光，随机态和文字态都有清晰的远近层次。

## 3.0.68 - 2026-08-20

- 欢迎页最底层新增低对比度青绿、深蓝与微紫色旋光星云，两组锥形渐变以 46 秒和 67 秒周期正反旋转，并叠加椭圆光环与中心暗部，增强粒子背景的深邃宇宙空间感。
- 星云层使用径向遮罩、44px 模糊和最高 42% 整体不透明度，文字粒子仍位于其上、全部 UI 位于遮罩上方；光效不接收指针事件且在“减少动态效果”模式下停止旋转。

## 3.0.67 - 2026-08-20

- 聚合文字升级为欢迎页主视觉：桌面文字目标扩展到画布宽度的 78%、高度的 48%，移动端扩展到宽度的 92%；粒子集中到 9px 字形描边骨架，并在左上、右下等空白区交替出现，避免巨大填充字因密度摊薄或被导入卡遮住而无法识别。
- 每轮文字旋转解体后，为全部粒子重新生成覆盖全屏的独立随机目标；粒子先从文字结构彻底分散并持续随机游动 10 秒，之后才开始用 2.4 秒聚合下一组文字。
- 随机态保留鼠标与触控排斥，但文字和粒子仍固定在 `z-index: 0` 的无事件 Canvas，欢迎页 UI 继续位于 `z-index: 3`，放大后的文字不会遮挡或拦截操作。

## 3.0.66 - 2026-08-20

- 欢迎页粒子聚合目标由女性轮廓改为十组循环文字：`4D`、`GS`、`WDD`、`SS`、`MoMo`、`4CGS`、`RAW4D`、`PLY4`、`WEBGPU` 和 `DONG`，长短文字会按侧边可用空间自动缩放并在左右、顶部与底部轮换。
- 每轮时间线改为先自由散开完整保持 5 秒，再用 2.4 秒聚合文字、保持 2.4 秒并用 1.6 秒旋转解体；页面首次打开同样先显示 5 秒自由粒子，不会立即进入聚合。
- 粒子数量在上一版基础上增加 50%：桌面按面积生成 450–780 个，移动端生成 315–420 个；同步降低像素密度与连接采样频率，控制增加后的绘制开销。
- 粒子 Canvas 固定在欢迎页独立堆叠上下文最底层且不接收指针事件，标题、导入区、相册及设备状态统一置于遮罩上方；导入卡增加 88% 深色隔离底与轻量模糊，高密度粒子不会透过卡片干扰文字和操作。

## 3.0.65 - 2026-08-20

- 欢迎页粒子数量从桌面最多 108 个提升为按画布面积生成 300–520 个，移动端提升为 210–280 个；自由状态增加独立速度、波浪游动和更明显的闪烁光晕，不再像静止装饰点。
- 全部粒子以 9.6 秒为周期循环执行“游动、聚合、保持、旋转散开”，依次生成侧颜长发、展臂舞者、礼服剪影、奔跑者、坐姿读者和芭蕾旋身六种抽象女性图案，并在左右与中央舞台间轮换。
- 聚合状态使用弹性吸附和呼吸偏移，散开状态加入径向旋转爆发；鼠标与触控排斥继续叠加在编舞上，减少动态效果时固定显示首个完整轮廓。

## 3.0.64 - 2026-08-20

- 欢迎页背景新增薄荷绿与青蓝色 Canvas 粒子场：粒子持续缓慢漂移并形成短距星座连线，鼠标或触控靠近时产生排斥、指针连线和脉冲光环，让空场景保持克制但可感知的互动。
- 粒子层完全在浏览器前端运行且不拦截导入、相册等操作；页面切到后台时暂停动画，移动端限制为最多 58 个粒子并降低像素密度，系统启用“减少动态效果”时只绘制静态画面。

## 3.0.63 - 2026-08-20

- 所有浏览器 4CGS 写入路径在 `metadata.editorBuild` 中保存 `schemaVersion`、`Dong Editor 3` 产品名和根 `VERSION` 注入的应用版本；新编码、无损 RAW4D Bundle 与 Save As 都会写入并回读校验，旧文件保持可读且压缩流原字节不变。
- 左下角 Gaussian 统计新增“关键帧”：单段场景合并 Position、Rotation、DC、Scale、Opacity 的唯一时间节点计数，多段 RAW4D 和 4CGS 按各段 bank/stride 还原全局关键帧后去重，避免把五种属性 bank 数量直接相加。
- 状态栏改为场景状态、统计、内存三列隔离布局，统计内容只能在自身列内横向滚动；窄桌面隐藏重复的“场景就绪”文字，避免新增统计卡遮挡内存和其他状态信息，手机播放器仍只显示高斯总数。

## 3.0.62 - 2026-08-20

- RAW4D 读取适配器新增 `vertex`/`vertex_static` 多 element 支持，兼容 `vertex:x`、`vertex_static:x` 限定 FP16 标记，并将动态点和静态点按连续稳定 ID 归一化到现有 Canonical RAM；真实 `master_v2_split.raw4d` 的 88,287 个动态点、24,712 个静态点和 24.895M 载荷通过完整 Worker/WASM 解析。
- 统一时间轨道语义覆盖 K=1 来源、首尾 K=2 全程线性和 K>2 分段线性：静态点记录为首尾端点严格相等的虚拟 K=2 轨道，长生命周期继续独立保存，未来输入格式只需在读取层映射显式关键帧时间。
- RAW4D 源压实支持分别删除动态/静态 element 中的稳定 ID，保留各自记录宽度和限定 FP16 注释；4CGS V2.6 内存导出在 metadata 中保存时间分区布局，实测 112,999 点导出为 6.258M、4.443x、24.79 秒并成功回读，无需升级 4CGS 外层容器。

## 3.0.61 - 2026-08-19

- 修复循环播放回到第 0 帧时的瞬时错序：时间轴在循环边界暂停推进，等待 PlayCanvas `frame:ready` 明确确认最新高斯中心已完成排序且无待加载资源后再恢复播放；超时只会继续等待，不再放行未排序帧。
- 增加手机播放器模式：移动设备或宽度不超过 720px 时，主菜单只保留相册，渲染栏只保留高斯/点，关闭网格、坐标轴、身高尺和外包络，并移除左侧编辑工具、选择详情、右侧检查器、插件窗口、直方图、导航立方体与桌面动作栏。
- 手机时间轴精简为播放、进度拖动、帧号和循环按钮，底栏只显示全局未删除高斯总数；390×844 实测 `red.4cgs` 可显示 335,265 点双片段场景，页面无 WebGL、Worker 或 React 警告。

## 3.0.60 - 2026-08-19

- 增加自动移动端运行档案：识别 Android、iPhone/iPad 和 iPadOS 桌面 UA 后，默认采用 768MB 系统内存 / 192MB 显存预算、单 Loader Worker、1× Canvas 像素密度、WebGL2 和 SH0；桌面端仍默认 32GB / 12GB 高驻留模式。
- 新增 WebGL2 滑动关键帧纹理后端，仅常驻当前插值所需的左右关键帧、一个预取关键帧与 DC 基准帧；纹理容量不再随片段总关键帧数增长，并保留动态位置排序、透明度、选择及删除掩码。
- 移动兼容档关闭未来片段 GPU 预取，预算切换会重建 GraphicsDevice 并重新上传当前场景；性能面板可手动选择“移动兼容”，并显示 `WebGL2 · 滑动关键帧` 作为已生效证据。
- 使用 `public/4cgs/red.4cgs` 的 335,265 点双片段场景实测：桌面 WebGL2 全量纹理约 120MB GPU，移动兼容档约 42MB，降低约 65%；394×760 Canvas 正常出图且无 WebGL/着色器错误。

## 3.0.59 - 2026-08-19

- 修复 Cover Flow 换位末端的几何跳变：侧卡与主卡统一为相同宽高比，并将进入动画终点精确对齐到主卡的 360×170 尺寸、左上坐标、圆角和纵深位置。
- 换位状态延后至 3D 动画完全结束后再提交，侧卡遮罩与“下一场景”标签在进入过程中渐隐，场景标题层在落位后以 260ms 渐入，减少内容层突然切换。
- 自动轮播改为基于当前场景和悬停状态重新调度的单次计时器：悬停可靠暂停，离开后重新等待 6.4 秒，避免旧计时器与手动翻页竞争。

## 3.0.58 - 2026-08-19

- 欢迎页相册从横向小叠卡重做为大幅 3D Cover Flow 舞台：当前场景以 360×170 全景卡悬浮在前景，下一场景以约 48° Y 轴透视角停靠在右后方，并增加地面光晕和镜面倒影。
- 相册每 6.4 秒自动演示一次三维换位，前景卡旋转退场、侧卡从纵深旋转进入前景；鼠标进入舞台后暂停自动轮播，离开后恢复，不再依赖用户主动拖拽才能发现效果。
- 保留鼠标视差、左右甩卡、侧卡点击、翻页按钮和完整相册入口；场景名称、页码与操作提示改为全景图上的分层浮动信息。

## 3.0.57 - 2026-08-19

- 相册牌组增加无需操作即可识别的空闲演示：每 5.2 秒前景卡片会轻微侧移倾斜，后方卡片同步探出并显示“下一张”，直接展示牌组层级。
- 将拖动说明升级为带双向动态箭头的高对比提示胶囊，并扩大后方卡片的常驻露出宽度；鼠标悬停或拖动时自动暂停演示，避免与用户操作冲突。
- 遵循 `prefers-reduced-motion`：系统要求减少动态效果时关闭空闲牌组动画和提示箭头动画，翻页按钮与静态层级仍然可用。

## 3.0.56 - 2026-08-19

- 欢迎页相册升级为 3D 景深牌组：鼠标在卡片上移动会产生透视倾斜与高光扫过，下一张快照在后方以景深叠放形式露出。
- 支持按住缩略图左右拖动甩卡翻页，44px 手势阈值避免误触；普通点击仍打开完整相册，并保留左右按钮和动态页码指示器作为精确操作入口。
- 交互完全使用浏览器 Pointer Events 和 CSS 3D 动画实现，不增加前端运行库或网络依赖，拖动过程中禁止图片原生拖拽。

## 3.0.55 - 2026-08-19

- 欢迎页相册入口升级为真实缩略图预览：直接读取 `4cgs/gallery.json`，显示当前场景快照、名称和页码，点击主体仍打开完整相册。
- 缩略图后方露出下一张叠放卡片，并增加上一张/下一张循环翻页按钮；相册清单读取失败时保留安静的占位入口，不阻塞本地文件导入。

## 3.0.54 - 2026-08-19

- 欢迎页重做为单任务居中布局：只保留场景导入主入口、相册文字入口和一行设备状态，去掉营销式大标题、双卡片和六宫格硬件面板。
- 导入区使用大面积留白与弱虚线边界，继续支持全页拖入和点击选择；工作区恢复信息改为低干扰的单行提示。
- 硬件检测继续真实执行，但结果压缩为兼容模式、渲染后端、共享内存、线程数和设备内存提示，不再争夺欢迎页视觉焦点。

## 3.0.53 - 2026-08-19

- 新增可折叠 Gaussian 数据直方图：支持透明度、最大尺度、体积、距原点、XYZ 位置和生命周期指标，可拖动图表或精确输入上下限，按稳定 ID 替换、添加或移除选择。
- 直方图区分“当前帧可见”和“全部”：前者仅统计当前片段当前帧中透明度可见且未删除的点；后者逐片段逐帧扫描全部未删除点，并可按时间最小值、平均值或最大值聚合后跨片段选择。
- 空场景升级为时尚欢迎页，提供文件点击/全页拖入、内置 4CGS 相册、工作区恢复提示，以及 WebGPU、WebGL2、SharedArrayBuffer、逻辑线程、设备内存提示和最大纹理尺寸检测。
- 欢迎页明确区分浏览器提供的设备内存提示与网页真实可分配量，引导用户通过性能面板压力测试获得实测上限；空场景不再显示无效时间轴。

## 3.0.52 - 2026-08-19

- 视角书签恢复改为 600ms 缓入缓出动画，距离、俯仰、观察中心和偏航同步插值，不再瞬间跳转摄像机。
- 偏航跨越 ±180° 时自动选择最短旋转路径；动画结束严格落到书签原始相机参数，避免累计误差。
- 鼠标旋转/平移/缩放、WASD+QE、导航立方体或新的相机操作会立即打断书签动画并交还控制；工作区载入仍即时恢复相机，不播放多余过渡。

## 3.0.51 - 2026-08-19

- 修复 F 聚焦只读取当前活动片段位集的问题：可见范围仍聚焦当前片段，全局范围会按需读取所有片段的已选 Gaussian，并用统一世界变换计算联合包围盒。
- 重做视角书签槽交互：空槽点击即保存、已保存槽点击即恢复，右侧回转按钮用于覆盖当前构图；槽位明确显示“保存/恢复”状态，不再以禁用编号误导用户。
- F 快捷键与场景面板共用异步聚焦链路，跨片段取数失败会显示具体应用内错误；空选择提示会明确区分当前片段与全局范围。

## 3.0.50 - 2026-08-19

- 4CGS 场景元数据新增三个 Orbit 视角书签槽位，保存 `.4cgs` 时与完整场景变换一并写入，并在写后重新解析逐值核验距离、俯仰、偏航和观察中心。
- 读取 4CGS 时恢复文件内的视角书签并立即启用已有槽位；打开无书签的旧 4CGS 或其他新场景会清空三个槽位，避免沿用上一场景视角。
- 视角书签采用带版本的 `playcanvas-y-up` 元数据结构，保持旧文件兼容，同时拒绝槽位数量错误、非有限相机参数和非正观察距离。

## 3.0.49 - 2026-08-19

- 将主页左上角版本徽标、入口 HTML 的 `app-version` 标记和构建生成的 `version.json` 统一绑定根目录 `VERSION`，每次版本变化都会生成新的入口脚本哈希。
- 新增构建前后版本契约校验：`VERSION` 必须与 `CHANGELOG.md` 最新条目一致，左上角必须直接显示注入版本，`docs/index.html`、`docs/version.json` 和构建入口脚本必须包含同一版本，否则构建直接失败。
- Git pre-commit 在自动维护版本和更新日志后继续执行主页版本校验，避免提交或静态发布时遗漏左上角版本更新。

## 3.0.48 - 2026-08-19

- 修复 4CGS/RAW4D 连续播放时 CPU 排序 Worker 与当前 GPU 动态属性错配的问题：每一帧仍以精确动态中心计算深度顺序，避免固定参考中心在侧视角造成高斯遮挡错误和“跑飞”。
- 为 PlayCanvas 统一 CPU 排序器增加 world version 与队列排空双重守卫：新一版中心提交后丢弃仍在途的旧版结果，并在全部已排队任务完成前不应用中间结果；浏览器实测 219,489 点场景播放到第 118 帧后暂停并连续观察 6.4 秒，画面不再发生人物局部消失后恢复。
- 基于最终排序路径的独立场景稳定第 0 帧重新生成 `music` 与 `red` 的 `640×400` WebP 相册快照；截图关闭 Grid、Axes 与检查器，并裁掉应用界面，避免 UI 遮挡及跨场景残留状态。

## 3.0.47 - 2026-08-18

- 重新扫描 `public/4cgs` 并将新增的 `music.4cgs` 纳入测试相册：清单记录 41,627,099 字节文件、124,249,026 字节源数据、源帧 600–749、150 帧、1 个片段和 219,489 个高斯，与文件内嵌 manifest 一致。
- 使用当前浏览器 SH3 高斯渲染器重新生成全部 `640×400` WebP 快照；截图时关闭 Grid、Axes 与检查器并选取中段动作，`music` 卡片按真实画面标注为红衣人物手碟演奏，`red` 卡片同步更新为无界面干扰的统一构图。
- 相册严格校验通过：`gallery.json` 完整覆盖 2 个 4CGS 文件，逐项核对字节数、帧区间、片段/高斯/删除统计、codec、快照格式、尺寸及最小体积；相册解析测试 3 项与生产构建通过，`docs/4cgs` 已同步发布文件、清单和快照。

## 3.0.46 - 2026-08-18

- 修复 Float32 PLY4 导出 `.4cgs` 时被 V2.6 内存编码器以“当前内存不是 FP16”拒绝的问题：编码 Worker 现在从 Float32 Canonical RAM 显式生成独立 FP16 工作副本，场景内存及导入源数据不会被原地改写。
- Float32 转换前逐值检查有限性和 FP16 可表示范围，异常会准确报告属性名与稳定 ID；4CGS 清单新增源标量类型、编码标量类型和精度策略，避免隐式降精度。
- 导出中心在 PLY4 的 `.4CGS` 选项中明确提示 Float32 → FP16 Worker 副本策略，并增加 Float32 PLY4 内存编码、源内存不变和清单策略回归测试。
- 支持多帧 PLY4 使用单个基础属性列表示静态旋转、DC、尺度或透明度：编码时只在工作视图中把同一 SoA 列映射到首尾两个 canonical bank，保持 150 帧常量语义且不复制场景 RAM。
- 实测 241.446351 MB `_600_749_svd_thick_hull_trim.ply4` 完成 V2.6 编码与清单回读：输出 40,578,743 字节，工作集压缩比 6.1234 倍，耗时 90.295 秒；输出重新导入浏览器后为 219,489 个高斯、150 帧，末帧正确对应源帧 749，全程无错误提示。
- 完整测试 219 项通过、7 项按环境跳过，TypeScript 类型检查与生产构建通过。

## 3.0.45 - 2026-08-18

- 主导出中心只保留 `.4CGS` 与 `PLY 序列` 两个目标，移除“当前 Mesh”和格式含糊的“完整场景”选项；GS2Mesh 插件自身的 Mesh PLY 导出入口不受影响。
- 将导入的 PLY4 正式纳入 `.4cgs` Canonical RAM 编码条件：单个或多段 PLY4 与 RAW4D 共用内存快照、软删除压实、场景变换元数据和浏览器内 V2.6 编码路径，不再错误回落为 `.ply4` 压实保存。
- 导出卡片固定显示 `.4CGS`，并明确标注当前输入格式到 `.4cgs` 的转换；PLY4 场景下 `.4CGS` 与 `PLY 序列` 均可选择。
- 修复重新导入同一个 PLY4 时工作区恢复早于运行时文件切换的竞争条件，不再先误报“工作区编辑状态与当前场景不匹配”再继续加载场景。
- 浏览器实测 241.446351 MB `_600_749_svd_thick_hull_trim.ply4` 无错误载入 219,489 个高斯和 150 帧；导出中心仅有 2 个目标，`.4CGS` 与 `PLY 序列` 均可用。完整测试 218 项通过、6 项按环境跳过，类型检查和生产构建通过。

## 3.0.44 - 2026-08-18

- 修复有限负 `lifetime_w` 导致整份 PLY4 被拒绝的问题：导入层现在仅拒绝 NaN/Infinity，有限负值保持源位不变并继续交给模型健康检查报告，避免裁剪或 SVD 数值边界点阻断完整场景导入。
- 对 `_600_749_svd_thick_hull_trim.ply4` 的 219,489 个点完成逐项结构扫描：文件长度、275 个 float32 属性、51 个位置关键帧、11 个旋转关键帧均完整；仅 105 个生命周期半宽为有限小负数，范围为 `-0.0398042` 至 `-0.00516109`。
- 浏览器实测该 241.446351 MB 文件成功进入 `ready`：219,489 个高斯、150 帧、4D 常驻 223 MB、GPU 233 MB；连续播放到第 74 帧并跳转第 149 帧均正常，无错误对话框。

## 3.0.43 - 2026-08-18

- 提升右侧“场景”面板可读性：工作区状态和场景根节点增至 12px，层级对象、快捷命令与视角书签增至 11px，辅助项和时间/片段说明不再低于 10px；同步增加树行、按钮和书签行高，避免高分屏下文字过小。
- 检查器顶部四个 Tab 改为真正的四列单行布局，页签文字由 7px 提升为 12px、高度由 25px 提升为 30px，防止第四项错误换行并保持 320px 面板内无横向溢出。

## 3.0.42 - 2026-08-18

- 建立真正的浏览器工作区恢复状态：IndexedDB 自动保存源文件名/大小/修改时间、当前帧、播放速度、渲染模式、SH 级别、完整场景变换、Orbit 相机、三组视角书签、Grid/Axes/身高尺/外包络可见性，以及全部片段的选择与软删除稳定 ID 位集；刷新后自动打开恢复页并列出需要重新导入的文件，导入身份完全一致的文件集合后自动恢复，防止把旧编辑错误套到另一场景。“新建工作区”会先清除恢复点再强制刷新。
- 右侧检查器新增默认“场景”Tab 和真实 Outliner：按场景、Gaussian、时间片段、GS2Mesh、灯光、辅助对象显示层级；片段行可直接跳到对应源帧，Gaussian/Mesh/Grid/Axes/身高尺/外包络显隐直接控制运行时实体，工作区保存/恢复状态在层级顶部持续可见。
- 重做导出入口：顶部和文件菜单统一打开导出中心，在同一对话框选择完整场景、逐帧 PLY 或当前 Mesh，并预先显示范围、片段/帧数、软删除数量、场景变换策略和输入大小；继续复用浏览器内 4CGS 编码、目录逐帧写入与 Mesh PLY 导出，不引入后端。
- 强化时间轴但保持默认简洁：4CGS/RAW4D 显示真实源帧编号、活动片段名、交替片段底色和片段节点；RAW4D 的位置/旋转/尺度/DC/Opacity 稀疏关键帧拆成五条可手动展开的微型轨道，默认收起避免长序列视觉拥挤。Outliner 片段跳转实测从源帧 270 正确跳到 300。
- 新增高频视口命令：`Home` 显示整个场景，`F` 聚焦当前片段的选中 Gaussian；Outliner 提供同功能按钮与三组可持久恢复的相机书签。聚焦通过真实世界包围盒更新 Orbit target/distance，不直接改写 PlayCanvas 相机造成控制状态分裂。
- 统一应用错误结构：Worker 中断、内存不足、文件不匹配、目录权限及未知错误均映射为“用户摘要 + 可执行恢复建议 + 可展开技术详情”，视口加载失败提供原地重新载入入口，主导出/模型健康/工作区错误复用相同提示框，不再直接把底层异常当作唯一说明。
- 完整测试 216 项通过、6 项按环境跳过，TypeScript 类型检查和生产构建通过；浏览器实测 21,392,186 字节 `red.4cgs` 的 2 片段/61 帧 Outliner、源帧跳转、导出中心、刷新恢复提示、同源重新导入自动恢复和 4CGS 格式标识均正常。

## 3.0.41 - 2026-08-17

- 新增主菜单一级“相册”入口和纯前端 4CGS 测试相册：从 `public/4cgs/gallery.json` 读取卡片说明、标签、编码、帧数、片段数、高斯数与压缩率，并以流式进度读取所选 `.4cgs` 后直接载入当前编辑器；入口不再重复占用右侧快捷操作区或文件子菜单。
- 为目录中现有 `red.4cgs` 创建浏览器真实渲染中段帧的 `640×400` 正面人物 WebP 快照与逐文件说明；实测文件 21,392,186 字节、源帧 270–330 共 61 帧、2 个片段、335,265 个编码高斯，文件/源数据压缩率 3.86×。
- 新增相册严格校验脚本：逐项解析 4CGS 内嵌 manifest 核对字节数、帧区间、片段/高斯统计与 codec，并验证 WebP 尺寸、最小体积和 `public/4cgs` 下 `.4cgs` 文件的清单全覆盖，新增测试文件时漏写快照或说明会直接失败。
- “文件 → 新建工作区”改为强制刷新当前页面，使 React 状态、Web Worker、WASM、GPU 资源及大型场景内存从浏览器层完整重建；浏览器实测刷新后返回未命名空场景，相册入口与弹窗仍可正常打开。
- 空工作区点击顶部或文件菜单中的“导出”会在任何系统文件选择器和编码流程之前停止，并通过应用内提示框显示“场景为空”；不再下载没有高斯数据的空工作区 JSON。

## 3.0.40 - 2026-08-17

- 所有实际输出 `.4cgs` 的入口改用浏览器“另存为”文件授权：编码前即可选择目录并修改文件名，默认从“下载”打开且只允许 `.4cgs`；可直接保存到“下载”根目录，不再申请整个目录的读写权限，也不会触发“包含系统文件”的目录限制。
- 4CGS 编码完成后直接写入用户批准的 `FileSystemFileHandle`，写入成功才关闭文件流并报告完成；取消另存为保持静默，选择器或文件写入失败则进入应用内错误框。
- 清除 `src` 中全部 `window.alert` 系统弹窗：4CGS 只读编辑、导出失败、PLY 序列不支持及模型健康错误统一使用最高层自定义提示框，支持 Esc /“确定”关闭并阻断摄像机输入。系统文件/目录授权窗口因浏览器安全要求继续保留。
- 左下角状态栏由 20px 增至 32px；场景状态提升到 12px，高斯统计标签/数值提升到 11/12px，并增强文字对比度、数字字重和状态圆点尺寸，活跃/当前帧/已删除数量不再以 6–7px 显示。

## 3.0.39 - 2026-08-17

- 所有场景文件导出入口现在会在读取或编码数据前立即暂停播放，包括顶部“导出”、文件菜单中的 `.4cgs 文件`、RAW4D / PLY4 压实保存以及空场景工作区 JSON，确保导出期间时间轴不会继续推进。
- `.ply 序列`在打开系统目录选择器前就暂停并固定当前帧；GS2Mesh 工作流中的“导出 PLY”也使用同一规则，避免插件导出时后台仍在换帧。

## 3.0.38 - 2026-08-17

- 修正导出 PLY 序列时直接选择“下载”根目录会被 Chrome / Edge 以“包含系统文件”拒绝却缺少解释的问题：导出前新增顶层目录说明框，明确要求在“下载”中创建或进入专用子文件夹，避免把浏览器安全限制误报为程序读写故障。
- 系统目录选择器现在首次直接从“下载”打开，并使用独立选择器 ID 记住用户已授权的专用输出目录；再次导出可回到该子目录。取消或被浏览器拒绝后保留说明框并给出可执行的恢复提示，非取消错误显示实际原因。
- 新增目录选择参数与错误分类回归测试，确认请求权限为 `readwrite`、初始目录为 `downloads`，并区分 `AbortError` 与真正的安全/写入错误。

## 3.0.37 - 2026-08-17

- 将独立的“GS2Mesh 当前帧重建”菜单入口合并到“GS2Mesh · Gaussian 重光照”插件，插件中心从 4 个卡片精简为 3 个；重建算法仍保持独立模块，但共享同一个 Mesh 代理、可见性状态和重光照控制器，不增加后端或重复数据。
- 合并后的弹窗新增两步工作流：Step 1 从当前帧生成 GS2Mesh 表面/法线代理，Step 2 添加、移动光源并把光照传递到 Gaussian；Step 2 在 Mesh 就绪前锁定，Step 1 首次成功后自动进入 Step 2，Mesh 被清除或重新生成时自动返回 Step 1。
- 两个步骤的子面板保持同时挂载，往返切换不会重置 GS2Mesh 精度、体素、平滑等输入；只有进入 Step 2 时才启用光源 Gizmo 编辑，避免在重建步骤误操作灯光。
- 新增绿到金色的步骤条、完成/运行/错误/锁定状态提示及中英文工作流文案，并增加步骤状态回归测试；完整测试为 203 项通过、6 项按环境跳过，生产构建通过。浏览器实测插件中心只显示一个组合入口，Step 1 默认激活、Step 2 正确锁定，弹窗无横向溢出。

## 3.0.36 - 2026-08-17

- 修正“全部”诊断模式把当前帧 Alpha 小于 `1/255` 的正常时序点染成红色的问题：低透明度、生命周期暂时关闭和合法 `opacity = -Infinity` 不再判为异常；红色现在只表示当前解码的位置/颜色含 NaN、Infinity 或超过 `±100000` 的运行时数值，且红点绝不直接作为删除依据。
- 模型健康插件把“检查”与“安全清理”严格分离：清理不再顺带改写红点属性或刷新 Canonical/GPU 数据，只软删除全部 opacity 关键帧均精确为 `-Infinity`、关键帧结构与生命周期证据完整且尚未删除的点；有限低透明度、单帧不可见、遮挡、视锥外、尺寸和证据不完整点全部保留。
- 健康检查与清理从当前片段扩展到整个序列，按片段载入、检查并立即释放外部数据；删除以跨片段原子历史命令写入，可撤销/重做，只有保存导出时才正式压实。
- 插件新增 ALL 绿/红语义说明、全片段/高斯检查数量和严格 Alpha=0 图示；没有安全候选时禁用清理按钮。真实检查 `segment_270_300.raw4d`（185,606 点）和 `master(1).raw4d`（577,292 点）均得到 0 个严格安全删除候选，因此不会误删这些文件中的红点。
- 新增多片段报告合并、ALL 透明度阈值隔离和可选真实文件安全检查回归；完整测试为 201 项通过、6 项按环境跳过，生产构建通过，本地浏览器确认插件说明、按钮状态、ALL 提示及控制台均正常。

## 3.0.35 - 2026-08-17

- 通过浏览器真实截图复现并修正高 DPI 根因：SVG 的 `vector-effect: non-scaling-stroke` 会把笔迹宽度按设备像素处理，在 `devicePixelRatio=1.25` 时将 `98px` 错误显示为约 `78.4px`；现已移除该属性，让笔迹宽度与圆圈光标一样按 CSS 像素渲染。
- 圆圈光标虽然元素盒为 `96px`，但 CSS 外扩环使真实可见外径为 `98px`；半透明拖动痕迹同步使用 `98px`，不再只比较名义 DOM 属性。
- 松开笔刷后将最终覆盖痕迹保留 `420ms`，便于直接核对痕迹和圆圈边界，并避免选择计算刚开始时视觉反馈立即消失；开始下一次操作、切换工具或取消时会立即清理。
- 屏幕尺寸回归测试同步区分 `96px` 命中直径与 `98px` 可见直径，防止后续再次把元素盒尺寸误当成最终渲染边界。

## 3.0.34 - 2026-08-17

- 修正 `v3.0.33` 为显示边缘而把半透明刷子痕迹直径缩小 `2px` 的问题：移除 `1px` 内缩和冗余外沿，痕迹本体现在直接使用完整命中直径。
- 默认半径 `48px` 时，圆圈光标、半透明拖动痕迹和实际选择区域均严格为 `96px` 外径；不再出现痕迹比正确圆圈光标小一圈的视觉差异。

## 3.0.33 - 2026-08-17

- 修正刷子拖动痕迹比实际选择范围小且中心偏移的问题：SVG 痕迹层现在直接贴合 Canvas 的 CSS 像素矩形，并按实时画布宽高设置 `viewBox`，不再受父级边框、页面缩放或默认 SVG 坐标系影响。
- 实际命中区域、圆形光标和拖动痕迹统一使用同一个屏幕半径计算；默认半径 `48px` 时，三者外径均为 `96px`。痕迹增加位于真实命中边界上的清晰外沿，内部半透明填充缩进 `1px`，便于准确判断覆盖范围。
- 新增屏幕尺寸回归测试，验证半径归一、直径计算和命中边界；类型检查与对应的 5 项选择区域测试通过。浏览器在 `1266.4×598.4` CSS 像素视口中实测，痕迹 `viewBox` 与画布一致，外沿 `96px`、填充 `94px`。

## 3.0.32 - 2026-08-17

- 变换检查器新增“重设模型原点”：确认后把当前世界平移、旋转和等比缩放写入全部驻留片段的 Canonical Gaussian 数据与每个位置/旋转/缩放关键帧，屏幕中的模型保持原位，实体变换随后归零为位置 `0/0/0`、旋转 `0/0/0`、缩放 `1/1/1`。
- 烘焙同时左乘每个高斯的椭球四元数、累加三轴对数尺度，并按 PlayCanvas 模型空间视线约定旋转 SH1/SH2/SH3 系数；DC、Opacity、生命周期、选择/删除稳定 ID 保持不变。非等比缩放会在写入前明确拒绝，因为有限阶 SH 和独立旋转/尺度关键帧无法无损表达方向扭曲。
- FP16 模型在烘焙前检查变换后包围盒是否超过 `±65504`，包围盒按真正写回的量化坐标重算；长循环每约 12ms 主动让出主线程，并在最高层确认框显示片段、属性阶段和总体进度。
- 多段烘焙会停止旧预取、释放非活动 GPU 片段并从修改后的系统内存按需重传；活动 WebGL2 CPU Sampler 同步重建旋转插值缓存和 SH 视图，GS2Mesh 顶点/法线也写入相同 TRS 后归一，避免切段、Mesh 或重光照代理仍使用旧坐标。
- Canonical 数据改写后，RAW4D/PLY4 导出不再回读原始文件载荷；已烘焙的 4CGS 会从全部内存片段重新编码并写入单位场景变换，未修改的 4CGS 继续使用快速无损另存路径。烘焙会清空不再适用于新坐标系的撤销/重做历史。
- 新增 4 项数学回归测试，验证多关键帧位置、四元数、对数尺度、SH1–SH3 方向等价性和 FP16 量化边界，并确认非等比缩放在任何 Canonical 数据变化前被拒绝；完整测试为 199 项通过、5 项按环境跳过，生产构建通过。浏览器实测 185,606 点 SH3 RAW4D：非零 TRS 在约 3.6 秒内写回并归零，首/中/末帧无控制台错误，烘焙前后画面位置一致。

## 3.0.31 - 2026-08-17

- 自动对齐的全部赤道和球面机位改为严格围绕用户当前 Orbit target 拍摄，沿用点击分析时中央正方形视口的世界跨度，不再重新跳到整份 RAW4D 的包围盒中心或自动缩放到全场景。
- 修正中央正方形识别截图与相机元数据跨度不一致的问题：横纵世界跨度现在都对应真实裁剪区域，避免宽屏视口把水平关键点距离错误放大。
- 增加极限强制兜底：只要至少一个视角存在可用身体轴，即允许按姿态关键点的脚到头有向语义扶正，不要求独立人脸命中；单视图脚点缺少深度交会时投影到当前 Orbit target 平面，结果仍可撤销并接受复检反证。
- 视口画布同步公开只读 `data-camera-target` 调试值，用于浏览器回归确认平移后的真实 Orbit target 已被多视角采集采用。
- 端点人脸裁剪由仅检测 0°/180° 扩展为 0°/90°/180°/270° 四方向，修复横置人物已有姿态但人脸被报告为 0 的问题，使单人脸强制兜底能够实际触发。
- 身体强制分支绕过多视角主方向占比和独立人脸门槛；有脸时仍用于增强语义证据，无脸时直接融合身体的脚到头有向轴，避免低质量面部投票把人物翻成倒立。
- 独立人脸网格在低纹理 Gaussian 脸上漏检时，使用姿态模型已确认的鼻子、眼睛和耳朵关键点生成低权重人脸证据；严格人脸结果仍优先，身体兜底不依赖这条补充证据。
- 浏览器实测 `master(1).raw4d`：Orbit target 保持为用户平移后的 `(0.88054, 1.24166, -0.68795)`，身体语义路径识别 1 人、36 个有效视角、58% 置信度，复检确认朝上且站立中心归零；正视图人工核验头在上、脚踩地。

## 3.0.30 - 2026-08-17

- 智能人物对齐在原始截图检测不到姿态时，新增 90°、-90°、180° 三方向的浏览器 Worker 本地旋转检测，覆盖横置和倒置人物，不增加网络或后端依赖。
- 旋转检测命中后将 33 个姿态关键点反变换回原始抓帧坐标，再参与多视角世界方向与站立中心求解，避免把旋转后的屏幕坐标误当成相机坐标。
- 多视角采集由单独的 16 机位水平环绕，扩展为 16 个高辨识度赤道机位加 24 个 Fibonacci 球面均匀机位，覆盖人物上方、下方和所有侧向；首次求解与对齐后复检使用同一套 40 机位全方位取证。
- 对场景级 RAW4D 增加语义加强门槛：至少 3 个机位的身体轴一致且至少 2 个机位的人脸头端一致性达到 80% 时可进入求解，仍拒绝缺少人脸语义或方向冲突的少视角结果。
- 浏览器实测 `master(1).raw4d` 从旧版 3/16 有效视角且拒绝写入，提升为识别 1 人、4 个有效机位、36% 融合置信度并保留首轮对齐；对照 `segment_180_210.raw4d` 为 1 人、12 个有效机位、55% 置信度且复检确认成功。

## 3.0.29 - 2026-08-17

- 模型健康检查将 opacity Logit 的 `-Infinity` 认定为合法的完全透明表示，检查和修复都保持其 IEEE 数值，不再报为非有限异常或钳制为 `-20`。
- 自动清理收紧为最安全规则：仅当某点全部 opacity 关键帧都精确为 `-Infinity`、生命周期证据完整有效时才做可撤销软删除；有限低透明度、NaN、`+Infinity` 和证据缺失全部保留。
- 插件新增删除安全证明流程图，明确展示扩展插值、`sigmoid(-Infinity) = 0`、完整时间轴零贡献以及不会采用的相机相关删除依据。

## 3.0.28 - 2026-08-17

- 人物对齐复检改为反证回滚：几何方向证据不足时保留已经通过首轮严格检测的对齐结果，不再恢复原变换。
- 只有可靠人脸语义明确证明倒立，或可靠几何轴仍存在超过 60° 的残余倾角时才回滚；复检不确定会在插件中显示保留原因。

## 3.0.27 - 2026-08-17

- 按需求移除右上角导出提示：删除 `exportNotice` 状态、顶栏 `.export-notice` 渲染块及其全部 CSS 样式，导出后右上角不再显示任何通知（含“已生成/已导出/失败”类消息）。
- 回退 3.0.25 为 `.4cgs` 另存与 PLY4/SOG/PLY 压实导出补充的通知与计时逻辑，恢复为静默下载；失败时仍用 `window.alert` 报错。
- RAW4D 保存与 `.ply 序列`导出的进度、结果与错误反馈完全保留在居中的“保存监督”弹窗中，不受影响。

## 3.0.26 - 2026-08-17

- 精简右上角导出成功通知：RAW4D 保存不再显示“已去掉 N 个删除点并生成…”前缀，改为直接显示“已生成 文件名 · 大小M · 压缩比×”；无删除编辑时该计数（如“已去掉 0 个删除点”）属于噪音。
- `.ply 序列`导出成功通知同步去掉“去掉 N 个删除点”片段，保留帧数、目标目录与输出大小。
- 编码过程中的阶段进度消息（如“正在读取并去掉删除点 i/N”）不受影响，仍如实反映压缩阶段。

## 3.0.25 - 2026-08-17

- 修复导出后右上角不显示导出信息的问题：`.4cgs` 无损另存、PLY4/SOG/PLY 压实导出和工作区 JSON 兜底导出三条路径此前只静默下载，现在统一接入 `exportNotice` 右上角通知，与 RAW4D / `.ply 序列` 路径一致。
- `.4cgs` 另存开始时显示进度通知，完成后显示“已无损另存 文件名 · 文件大小（M） · 耗时（s）”；失败从 `window.alert` 改为错误通知，避免弹窗被浏览器策略吞掉。
- PLY4/SOG/PLY 压实导出逐进度显示“已写点位/总点位 · 百分比”，完成后显示文件名、大小与耗时，失败改用错误通知。
- 工作区 JSON 导出完成后显示文件名与大小通知。

## 3.0.24 - 2026-08-17

- `.ply 序列`导出不再打包 ZIP：点击后先用 File System Access API（`showDirectoryPicker`，readwrite 模式）选择本地目录，再由导出 Worker 通过 `FileSystemDirectoryHandle` 逐帧直写 `frame_XXXXXX.ply`；不支持的浏览器明确报错，不改用其他通道。
- `.ply 序列`导出复用 4CGS 保存监督弹窗：显示阶段、逐帧进度条、最近日志、耗时、输出字节数和帧文件数，可随时取消；取消会终止 Worker 并在弹窗与日志中说明目录中可能保留已写入的部分帧。
- 导出器重构为 `createRaw4DPlySequenceEncoder` 逐帧编码器（段落行缓冲按需分配、切换段落即可回收），移除 fflate ZIP 依赖，PLY 导出 Worker 从 18.24 kB 减到 7.54 kB；插值与压实语义不变，11 个单元测试全部改跑编码器 API 并保持通过。

## 3.0.23 - 2026-08-17

- 文件菜单“导出”升级为二级子菜单，提供“.4cgs 文件”与“.ply 序列”两个入口，悬停展开。
- `.ply 序列`按主时间轴逐帧求值标准静态 3DGS float32 PLY（xyz 线性、WXYZ SLERP、DC 线性、raw log-scale 原值、opacity 先 sigmoid×生命周期门再回写 logit），多段共享边界只输出一次并由后一段 local=0 接管，帧号沿用文件名源帧范围的全局编号，已删除高斯逐帧压实。
- 序列在独立 Worker 内以 fflate 流式 ZIP 打包为单个 `*-ply-sequence.zip` 下载并逐帧回报进度；语义对齐 `py/export_raw4d_to_ply_frames.py`，无新增网络或后端依赖。

## 3.0.22 - 2026-08-17

- 在项目级开发要求中固化“每次修改必须同步递增 `VERSION` 并在 `CHANGELOG.md` 写入具体条目”的规则，与既有 pre-commit 自动校验（`.githooks/pre-commit` → `scripts/update-version.mjs`）保持一致。

## 3.0.21 - 2026-08-17

- 保存监督弹窗禁用横向滚动，阶段信息、日志和耗时卡片在宽度不足时自动换行或排成多行。
- 弹窗和日志区的垂直滚动条统一为主检查器的 6px 绿色细滚动条，并保留稳定 gutter 避免布局抖动。

## 3.0.20 - 2026-08-16

- 4CGS V2.6 导出新增六片段并行 SH WASM 确定性内核，保持与 JavaScript 回退相同的 f64 标签和误差判定，并在不支持共享内存时安全回退。
- Position 正式封装绕过会被立即解开的临时 rANS 往返，保留旧码流 API 与完整回读验证；六段 335.223M RAW4D 两次实测为 24.6–25.1 秒，输出保持 77.880M、4.30×。
- 保存监督框记录 SH 后端、WASM Worker 数量和真实峰值并发；13980HX 实测保留四路 Position Brotli，避免六路争抢内存带宽导致回退。

## 3.0.19 - 2026-08-16

- 性能面板新增可用内存压力测试入口，可选择 256MB、512MB、1GB、2GB、4GB、8GB、16GB 或 32GB 额外分配上限。
- 测试在独立 Worker 中以 64MB 分块申请，并逐个 4KiB 页面写入以实际提交内存；持续显示已确认容量、进度和耗时，可随时取消。
- 完成、失败或取消后立即终止 Worker 释放全部临时内存；结果明确记录为当前会话的可用下界，不把测试上限描述为系统永久容量。

## 3.0.18 - 2026-08-16

- 性能面板新增运行时策略核验：只有运行时回报的模式及 32GB/12GB 预算全部一致时，才显示“已生效”。
- 新增浏览器内存实况，使用 `measureUserAgentSpecificMemory()` 显示包含 Worker 的网页总内存估算，并区分 JS 堆上限、浏览器近似设备内存、4D 数据预算及各自余量。
- 对网页无法读取的系统物理可用内存和显卡物理显存明确显示“浏览器未开放”，不再把应用缓存预算冒充硬件容量。

## 3.0.17 - 2026-08-16

- 应用启动默认使用“高内存・高显存”模式，初始系统内存预算为 32GB、显存缓存预算为 12GB，并预加载全部关键帧。
- 初始内存统计与运行时预算使用同一默认常量，消除首帧显示自动模式预算后再跳变的问题；仍可在性能面板切换到低预算模式。

## 3.0.16 - 2026-08-16

- 左上角版本徽标升级为更新信息入口，点击后显示当前版本改动与可展开的历史版本记录。
- 更新面板直接读取 `CHANGELOG.md`，版本内容随发布记录自动更新；支持关闭按钮、遮罩点击和 Esc 关闭，并隔离摄像机输入。

## 3.0.15 - 2026-08-16

- 性能面板新增网页内“高内存・高显存”预设，使用 32GB 系统内存预算与 12GB 显存缓存预算，无需浏览器启动脚本。
- 启用前显示顶层风险确认；确认后保持全部片段并行解码和系统内存驻留，同时立即扩大未来片段 GPU 缓存预算。

## 3.0.14 - 2026-08-16

- 左下角新增活跃高斯、当前帧显示高斯和已删除高斯三组实时数量。
- 活跃与删除数量覆盖全部片段；当前帧显示数只统计当前活动片段中未删除的高斯，避免跨片段重复扣除。

## 3.0.13 - 2026-08-16

- 身高尺重做为面向摄像机的精致单侧量尺，缩短并弱化 1 厘米细刻度，突出 10 厘米主刻度与整米标签。
- 尺中新增拖动柄，可在地面平面移动整把量尺，拖动过程不会带动摄像机。
- 身高尺改为默认隐藏，重新显示后保留当前移动位置。

## 3.0.12 - 2026-08-16

- 坐标轴恢复为红、绿、蓝三轴各 1 米；人物高度比对改用与坐标轴分离的 2 米暖色身高尺。
- 身高尺每 1 厘米一个刻度、5 厘米和 10 厘米分级加长，每 10 厘米显示数字；顶部工具栏和“视图”菜单均可独立显隐。

## 3.0.11 - 2026-08-16

- 变换检查器新增独立的 Mesh 显示/隐藏开关，与 GS2Mesh 及重光照插件共用同一可见状态。
- 未生成 Mesh 时开关自动禁用；切换仅影响重建网格，不改变高斯或有效点外包络。

## 3.0.10 - 2026-08-16

- 将整体 AABB 升级为带半透明面的凸外包络 Mesh，只扫描未删除高斯并覆盖全部动画位置关键帧。
- 删除、圆柱保留、健康修复、撤销和重做触发防抖重算；多段 RAW4D/4CGS 使用统一稳定 ID 删除位集合并外包络。
- 外包络采用 26 个对称支撑平面，零拷贝分块扫描 Canonical 轨迹，保证有效点位于网格内部且不会复制大体积属性数据。

## 3.0.9 - 2026-08-16

- 变换检查器新增独立的高斯整体包围盒开关，默认关闭；橙色三维边框始终可见，用于检查远离主体的杂点。
- 单文件覆盖全部动画关键帧范围，多段 RAW4D/4CGS 合并全部片段范围，并随场景位置、旋转和缩放同步变换。

## 3.0.8 - 2026-08-16

- 4CGS meta 完整保存位置、欧拉旋转与三轴缩放；导入通过运行时原子恢复到首帧实体和变换检查器。
- 保存后重新解析并逐项核验 TRS，二次另存继续保留未知 meta 字段和全部原始压缩流字节。

## 3.0.7 - 2026-08-16

- 坐标轴由单像素线段改为具有真实世界尺寸的三维轴杆；X/Z 为 1m，Y 轴中心 0.75m且总高严格为 1.5m。

## 3.0.6 - 2026-08-16

- 绿色 Y 轴改为从原点精确延伸至 1.5m，消除地面防闪偏移造成的 0.006m 长度误差。

## 3.0.5 - 2026-08-16

- 修正场景坐标轴无光照材质的顶点色通道，X/Y/Z 现在明确显示为红、绿、蓝三色。

## 3.0.4 - 2026-08-16

- ViewCube 改为实时同步轨道摄像机姿态的真实 3D 透视立方体，使用面法线、深度排序和多边形拾取。
- 点击六个方向面可吸附到正交视角；在立方体上拖动可直接环绕视图，并保持主视口输入隔离。

## 3.0.3 - 2026-08-16

- 新增小型 ViewCube 快速视角栏，可切换正前、正左、正右和正上视角，并保留当前观察中心与距离。

## 3.0.2 - 2026-08-16

- 下拉弹层改为页面顶层 Portal，并按触发按钮的视口位置定位，避免被选择面板、检查器或插件窗口遮挡。
- 所有 Tip 改为页面顶层 Portal；渲染模式工具栏的提示统一向下显示。
- 所有文本输入在拖动时屏蔽浏览器原生系统拖拽，同时保留文本选择与数值 Scrub。

## 3.0.1 - 2026-08-16

- 修复数值文本框普通点击、输入和选字误进入水平拖拽的问题。
- 文件菜单补齐导入与导出入口，简化顶部导出提示。
- 圆柱选择轮廓改为始终可见，并在渲染器就绪后可靠恢复工具状态。
- FPS 档位改为编辑器自绘下拉，弹层使用统一深色玻璃样式。
- “全部”模式使用绿色正常点/红色异常点绘制，点模式增加蓝色中心标记。
- 继续验证 1.5m 向上坐标轴以及 4CGS 完整 TRS 元数据保存、读取与应用。

## 3.0.0 - 2026-08-16

- 新增左上角版本徽标，并在 Vite 构建阶段从根目录 `VERSION` 注入版本号。
- 精简变换检查器，固定使用世界空间并保留位置、旋转、缩放数值编辑。
- 播放栏增加零填充的当前帧/总帧数，并在轨道中显示逐帧刻度。
- 播放速度增加 1、2、4、10、15、30、60 FPS 固定档位，默认使用 30 FPS。
- 变换数值框支持水平拖拽微调；所有数值文本框改为完整输入后再校验、按字段阈值修正。
- 新增圆柱选择工具，支持可见/全局扫描、选择圆柱内以及保留圆柱内外，并接入软删除与撤销。
- 网格拆为 1m 粗层和 0.5m 细层；X/Z 坐标轴为 1m，向上 Y 轴为 1.5m。
- 点模式缩小为原来一半并固定不透明；“全部”模式改为绿色正确点/红色异常点诊断。
- 统一下拉菜单与提示浮层样式；4CGS 清单可保存并恢复完整位置、欧拉旋转和缩放。
- 增加提交时自动递增补丁版本和生成修改说明的仓库钩子。
