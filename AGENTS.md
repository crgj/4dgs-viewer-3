# 项目级开发要求

## 插件运行边界

- 所有插件必须在浏览器前端完成核心功能，包括计算、重建、推理、数据转换和结果导出。
- 插件不得要求用户额外启动 Node.js、Python、CUDA、HTTP 或其他独立后端服务，也不得把 `localhost` 服务或云端 API 作为必需运行条件。
- Web Worker、WebAssembly、WebGPU、WebGL、IndexedDB、Cache Storage 和 OPFS 均属于允许的前端实现方式。
- 若浏览器能力不足，插件必须给出清晰的兼容性错误；不得静默切换到远程服务。

## 网络模型与资源

- 尽量避免依赖体积巨大的网络模型、权重文件或运行时资源；优先选择无需模型的算法、小型模型、量化模型或项目内置资源。
- 确实需要下载模型或大型资源时，必须显示下载状态、文件大小、版本和失败原因。
- 下载完成的资源必须持久保存在浏览器中，优先使用 Cache Storage、IndexedDB 或 OPFS，并通过版本号或内容哈希复用；页面刷新和再次启动插件时不得重复下载相同版本。
- 如果浏览器持久缓存不适合该资源，应将资源下载并固定到本项目的本地静态资源目录，通过本地 URL 加载，避免运行时反复访问外网。
- 缓存升级必须先确认新版本完整可用，再清理旧版本；不得在下载失败时破坏当前可用缓存。

## 插件验收

- 插件验收必须确认核心流程在没有独立服务的情况下可运行。
- 对需要外部资源的插件，至少验证首次下载、刷新后缓存命中、离线复用和版本升级行为。
- 开发说明必须列出网络依赖、资源大小、缓存位置、缓存键或版本策略，以及用户清理缓存的方法。

<!-- #WDD-gpt 2026-08-15 - 将纯前端执行和大型模型持久缓存确立为所有插件的项目级约束。 -->
